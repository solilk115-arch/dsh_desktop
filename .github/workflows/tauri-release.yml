name: Tauri Release

# Tauri 版三平台发布流水线：
#   推 v* tag（如 v0.5.0）→ 云端构建 Windows/Linux/macOS 安装包 → 上传 Release
#   手动触发（workflow_dispatch）→ 指定版本号补传
#
# 与旧 Electron release.yml 的区别：走 Tauri CLI（非 electron-builder），
# 产物为 NSIS/AppImage/dmg + Windows 便携版 zip；依赖 stage-payload.sh 组装内核 payload。
#
# 产物命名（统一 DSH-Desktop-[类型]-[版本]-[平台]-[架构] 格式，对齐 Electron 版历史命名）：
#   Windows x64  NSIS   → DSH-Desktop-Setup-<ver>-win-x64.exe
#   Windows x64  便携版 → DSH-Desktop-Portable-<ver>-win-x64.zip（解压即用，数据存 exe 旁 data/）
#   Windows ARM64 NSIS  → DSH-Desktop-Setup-<ver>-win-arm64.exe（实验性，continue-on-error）
#   macOS ARM64  dmg    → DSH-Desktop-<ver>-macos-arm64.dmg
#   Linux x64    AppImage → DSH-Desktop-<ver>-linux-x64.AppImage
#   Linux x64    deb    → dsh-desktop_<ver>_amd64.deb（deb 标准命名）
#   自动更新校验边车 → 每个主资产附带 <主资产名>.sha256（publish job 统一生成，
#   内容=sha256sum 格式「<64位小写hex>  <文件名>」，壳侧更新器解析取首段 hex；
#   Gitee 镜像 job 一并镜像，镜像后由 verify-update-sources.mjs 做双源核验）
#
# 用法：git tag v0.5.1 && git push origin v0.5.1   ← 正式发版唯一入口
#        手动 dispatch 仅用于给【已存在的 tag】补传/重建资产：流水线会强制
#        checkout refs/tags/v<版本>，绝不从分支头构建——v0.5.0 事故中分支头
#        已 bump 到 0.5.1，从分支头重建产出了「文件名 0.5.0 / 内嵌 0.5.1」
#        的版本混装资产。版本一致性的四道闸（逐级收口）：
#          ① checkout 强制 tag ref（本文件每个 build job）
#          ② tauri.conf.json version == tag 版本 fail-fast（每个 build job）
#          ③ 产物文件名内嵌版本断言（NSIS/deb/AppImage/dmg + Info.plist）
#          ④ publish 汇总断言：所有资产文件名含版本 + 体积 >50MB + 上传核对
#        完整发版步骤见 .github/RELEASE_RUNBOOK.md

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: '目标 release tag（如 v0.5.1；tag 必须已存在——将强制检出该 tag 构建）'
        required: true

permissions:
  contents: write

# 同一 ref 的发布构建串行排队、不互相取消（发布中途中砍产物得不偿失）
concurrency:
  group: tauri-release-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  build-windows:
    name: Windows x64 (NSIS)
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - name: Resolve version and ref
        # workflow_dispatch 补传必须构建 tag 对应提交——分支头往往已 bump 到
        # 下一版本，从分支头重建曾产出「文件名 v0.5.0、Info.plist 0.5.1」的
        # 混装资产。tag push 时 github.ref 本就指向 tag，行为不变。
        id: ver
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
            echo "ref=refs/tags/$tag" >> "$GITHUB_OUTPUT"
          else
            tag='${{ github.ref_name }}'
            echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.ver.outputs.ref }}

      - name: Assert tauri.conf.json version == tag version
        # 版本一致性第二道闸：安装包内嵌版本唯一来源是 tauri.conf.json 的
        # version 字段，与期望不符立即 fail-fast。
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED='${{ steps.ver.outputs.version }}'
          ACTUAL=$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' dsh-tauri/src-tauri/src/app/tauri.conf.json | head -1)
          if [ -z "$ACTUAL" ]; then
            echo "::error::无法从 dsh-tauri/src-tauri/src/app/tauri.conf.json 解析 version 字段"
            exit 1
          fi
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::版本混装防线：期望 ${EXPECTED}，但检出的 tauri.conf.json version=${ACTUAL}——请在 tag 对应提交上修正版本后重打 tag，或更正 dispatch 输入"
            exit 1
          fi
          echo "OK: tauri.conf.json version=${ACTUAL} == 期望 ${EXPECTED}"

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            dsh-tauri/src-tauri/target
          key: rust-win-${{ hashFiles('dsh-tauri/src-tauri/Cargo.lock') }}
          restore-keys: rust-win-

      - name: Install dsh-desktop dependencies
        run: cd dsh-desktop && npm ci --ignore-scripts
        shell: pwsh

      - name: Download vendor node
        run: |
          mkdir -Force dsh-desktop\vendor\node | Out-Null
          Invoke-WebRequest -Uri 'https://nodejs.org/dist/v24.15.0/win-x64/node.exe' -OutFile dsh-desktop\vendor\node\node.exe
        shell: pwsh

      - name: Download vendor npm
        run: |
          $tmp = "$env:TEMP\npm-pkg"
          mkdir -Force $tmp | Out-Null
          Invoke-WebRequest -Uri 'https://registry.npmmirror.com/npm/-/npm-10.9.2.tgz' -OutFile "$tmp\npm.tgz"
          tar -xzf "$tmp\npm.tgz" -C $tmp
          mkdir -Force dsh-desktop\vendor\npm | Out-Null
          Copy-Item "$tmp\package\*" dsh-desktop\vendor\npm\ -Recurse -Force
        shell: pwsh

      - name: Download rc7 client packages (for compat)
        run: |
          set -e
          cd dsh-desktop
          rc7_root="dist/win-unpacked/resources/app/node_modules"
          mkdir -p "$rc7_root"
          # PD1 对账（v0.5.2 复盘）：6 个 rc.7 客户端包之外，build-client-compat.mjs
          # 的闭包 BFS 还需要 web-react 的 use-sync-external-store@1.2.0 与
          # schema-form 的 @deepseek-ai/schemastery@3.18.1——v0.5.2 只下 6 个，
          # compat 构建抛「闭包包缺失」被非阻断 WARN 吞掉 → 官方包缺
          # client-compat.js（插件端 missed the module table）。闭包全集 10 包
          # （+ schemastery 的 @standard-schema/spec@1.1.0 / @deepseek-ai/cosmokit@1.8.2）。
          for spec in "@deepseek-ai/dsh-client-web-react@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-schema-form@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-primitives@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-slots@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-attachment@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-renderer@0.1.0-rc.7" \
                      "use-sync-external-store@1.2.0" \
                      "@deepseek-ai/schemastery@3.18.1" \
                      "@standard-schema/spec@1.1.0" \
                      "@deepseek-ai/cosmokit@1.8.2"; do
            name="${spec%@*}"
            echo "Downloading $spec ..."
            tgz=$(npm pack "$spec" 2>&1 | tail -1) || { echo "WARN: $spec not found, skipping"; continue; }
            if [ ! -f "$tgz" ]; then echo "WARN: $spec tarball not found ($tgz), skipping"; continue; fi
            tmp=$(mktemp -d)
            tar -xzf "$tgz" -C "$tmp"
            mkdir -p "$rc7_root/$name"
            cp -r "$tmp/package/"* "$rc7_root/$name/"
            rm -f "$tgz"
            echo "  OK: $name"
          done
          echo "rc7 packages ready (6 client pkgs + 4 closure deps)"
        shell: bash

      - name: Stage payload (CI simplified)
        run: |
          set -e
          DEST="dsh-tauri/package-payload/dsh-desktop"
          mkdir -p "$DEST"
          cp dsh-desktop/*.js "$DEST/" 2>/dev/null || true
          cp dsh-desktop/package.json "$DEST/"
          cp -r dsh-desktop/scripts "$DEST/scripts"
          cp -r dsh-desktop/assets "$DEST/assets"
          mkdir -p "$DEST/vendor"
          cp -r dsh-desktop/vendor/node "$DEST/vendor/node"
          cp -r dsh-desktop/vendor/npm "$DEST/vendor/npm" 2>/dev/null || true
          # vendored 内核 tarball（随仓库提交）：0.5.7 起 boot 链 compat-pin 步骤
          # fail-closed 校验 vendor/dsh-kernel 与 kernel-pin 一致，缺目录即拒绝启动
          # （0.5.7 本地打包踩坑复盘：五平台 stage 清单均已补齐，勿再删）。
          cp -r dsh-desktop/vendor/dsh-kernel "$DEST/vendor/dsh-kernel"
          rm -rf "$DEST/node_modules"
          cp -r dsh-desktop/node_modules "$DEST/node_modules"
          rm -rf "$DEST/node_modules/electron" "$DEST/node_modules/electron-builder" "$DEST/node_modules/electron-winstaller" "$DEST/node_modules/@electron" "$DEST/node_modules/@img/sharp-darwin-arm64" "$DEST/node_modules/@img/sharp-libvips-darwin-arm64" "$DEST/node_modules/@img/sharp-wasm32" "$DEST/node_modules/@koromix/koffi-darwin-arm64"
          # Codex/Claude 原生二进制（optional platform packages，~700MB 死重）：内核子代理
          # 适配器仅在真正拉起 Codex/Claude Code 进程时才解析这些 optional 二进制；import
          # 只读 JS 侧 metapackage。glob 精准命中各平台 variant（codex-win32-x64 /
          # codex-linux-x64 / claude-agent-sdk-darwin-arm64 ...），保留 metapackage 不断 import 图。
          rm -rf "$DEST"/node_modules/@openai/codex-* "$DEST"/node_modules/@anthropic-ai/claude-agent-sdk-*
          # rc7 vendor（非阻断——renderer 三级回落链兜底）
          if [ -d "dsh-desktop/dist/win-unpacked/resources/app/node_modules/@deepseek-ai" ]; then
            for pkg in dsh-desktop/dist/win-unpacked/resources/app/node_modules/@deepseek-ai/*/; do
              name=$(basename "$pkg")
              [ -d "$DEST/node_modules/@deepseek-ai/$name" ] || cp -r "$pkg" "$DEST/node_modules/@deepseek-ai/$name"
            done
          fi
          # compat（构建报错可不阻断——但产物必须在位，见下方 PD1 门禁）
          node dsh-tauri/scripts/build-client-compat.mjs 2>&1 || echo "WARN: compat failed (non-blocking)"
          # PD1 复盘门禁：v0.5.2 的 compat 闭包缺包被上一行 WARN 静默吞掉，官方
          # 包缺 client-compat.js（插件端 missed the module table 常态化）。
          # 构建器允许降级告警，装出去的包不允许缺件——缺即拒绝发布。
          COMPAT="$DEST/node_modules/@deepseek-ai/dsh-web-frontend/dist/assets/client-compat.js"
          if [ ! -f "$COMPAT" ]; then
            echo "::error::client-compat.js 缺失（build-client-compat 闭包失败——rc7 下载缺包？）"
            exit 1
          fi
          echo "OK: client-compat.js in payload ($(stat -c%s "$COMPAT") bytes)"
          echo "Payload: $(du -sm "$DEST" | cut -f1)MB"
        shell: bash

      - name: Run tests (unit only, skip env-dependent integration)
        run: |
          cd dsh-tauri/src-tauri
          # GHA 的 shell: bash 自带 -e（errexit）——失败管道会立刻终止本步骤，
          # 下面的注解透出逻辑永远不执行（前两轮注解为空的根因）。显式关掉。
          set +e
          cargo test --workspace --quiet -- --skip sidecar_boot_sandbox --skip full_boot_to_kernel --skip broken_companion --skip corrupted_patch 2>&1 | tee test-output.txt
          code=${PIPESTATUS[0]}
          # CI 日志下载域名（results-receiver.actions.githubusercontent.com）直连
          # 被墙——失败用例名以 ::error:: 注解透出（注解走 api.github.com 加速
          # 通道，是墙内环境唯一可读的诊断出口）
          grep -E "FAILED|panicked at|^error" test-output.txt | head -15 | sed 's/^/::error::/' || true
          tail -22 test-output.txt | sed 's/^/::error::TAIL /' || true
          exit $code
        shell: bash

      - name: Assert bundled D3DCOMPILER_47.dll (B1 fix prerequisite)
        # B1 修复：WebView2 在缺少系统 D3DCompiler_47.dll 的机器（LTSC/精简版/
        # Server 无桌面体验）上加载失败 → 应用进程入口报「丢失 D3DCOMPILER_47.dll」。
        # 修复=随包旁路分发（tauri.conf.json resources → 安装到 exe 同目录，Windows
        # 加载器优先应用目录）。此处构建前断言源 DLL 在位且完整，防静默缺失。
        shell: bash
        run: |
          set -euo pipefail
          f="dsh-tauri/dlls/D3DCOMPILER_47.dll"
          [ -f "$f" ] || { echo "::error::缺少 $f（B1 修复依赖）"; exit 1; }
          size=$(stat -c%s "$f")
          if [ "$size" -lt 4000000 ]; then
            echo "::error::$f 体积异常（${size} 字节，预期 ~4.6MB）"
            exit 1
          fi
          node dsh-tauri/scripts/check-imports.mjs --pe "$f" --expect-machine x64
          echo "OK: $f ($size bytes)"

      - name: Build installer
        run: |
          cd dsh-tauri
          echo "=== Verifying prerequisites ==="
          ls -la src-tauri/src/app/tauri.conf.json
          ls -la src-tauri/src/app/icons/icon.ico
          ls -la src-tauri/src/app/nsis/installerHooks.nsh
          ls -la ui/index.html
          ls package-payload/dsh-desktop/package.json
          echo "=== Building ==="
          # 不加 --verbose：rustc 全量 verbose 日志极大，失败时下方兜底分支会
          # 单独重跑 cargo build 取尾部错误，诊断信息不丢。
          npx --yes @tauri-apps/cli@latest build --config src-tauri/src/app/tauri.conf.json --target x86_64-pc-windows-msvc 2>&1 || {
            echo "=== BUILD FAILED ==="
            echo "=== Checking cargo build separately ==="
            cd src-tauri && cargo build --release --target x86_64-pc-windows-msvc -p dsh-tauri-app 2>&1 | tail -30
            echo "=== Exe exists: $(ls target/x86_64-pc-windows-msvc/release/dsh-tauri-app.exe 2>/dev/null) ==="
            exit 1
          }
        shell: bash

      - name: Rename installer (assert version + size)
        shell: bash
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          cd dsh-tauri/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis
          ls -la
          # 版本混装防线②：Rust cache 恢复的 bundle 目录可能残留上个版本的改名
          # 产物——上传 glob 是 DSH-Desktop-Setup-*，非本版本的旧文件必须清掉
          #（0.5.6/0.5.7 混装被 verify 拒发的实爆）。
          find . -maxdepth 1 -name 'DSH-Desktop-Setup-*' ! -name "DSH-Desktop-Setup-${VERSION}-win-x64.exe" -delete
          # 版本混装防线③：Tauri NSIS 产物名内嵌 tauri.conf.json 的版本
          # （<productName>_<ver>_x64-setup.exe）——期望版本不符时 glob 落空即失败
          shopt -s nullglob
          src=( *_"${VERSION}"_x64-setup.exe )
          if [ ${#src[@]} -eq 0 ]; then
            echo "::error::未找到 *_${VERSION}_x64-setup.exe——NSIS 内嵌版本与期望不符（见上方目录清单）"
            exit 1
          fi
          cp "${src[0]}" "DSH-Desktop-Setup-${VERSION}-win-x64.exe"
          size=$(stat -c%s "DSH-Desktop-Setup-${VERSION}-win-x64.exe")
          if [ "$size" -lt 52428800 ]; then
            echo "::error::安装包体积异常（${size} 字节 < 50MB）——疑似 payload 缺失（v0.5.0 参考：~72MB）"
            exit 1
          fi
          echo "OK: DSH-Desktop-Setup-${VERSION}-win-x64.exe ($size bytes)"
          ls -la

      - name: Assert D3DCOMPILER_47.dll inside NSIS installer (B1 fix gate)
        # B1 修复门禁（追加）：解包安装包断言 D3DCOMPILER_47.dll 已随包打进去，
        # 位于 exe 旁（$INSTDIR 根）——防 resources 配置回退导致用户精简系统再次
        # 撞上「丢失 D3DCOMPILER_47.dll」。NSIS 安装包可用 7z 直接列内容。
        shell: bash
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          f="dsh-tauri/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/DSH-Desktop-Setup-${VERSION}-win-x64.exe"
          [ -f "$f" ] || { echo "::error::未找到 $f"; exit 1; }
          7z l "$f" | grep -i "D3DCOMPILER_47.dll" || {
            echo "::error::NSIS 安装包内缺少 D3DCOMPILER_47.dll——B1 修复未生效（检查 tauri.conf.json resources）"
            exit 1
          }
          echo "OK: D3DCOMPILER_47.dll 已随安装包分发"

      - name: Upload setup artifact
        uses: actions/upload-artifact@v4
        with:
          name: win-x64-setup
          path: dsh-tauri/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/DSH-Desktop-Setup-*-win-x64.exe
          if-no-files-found: error

      - name: Upload raw exe artifact (for portable job)
        uses: actions/upload-artifact@v4
        with:
          name: win-x64-bin
          path: dsh-tauri/src-tauri/target/x86_64-pc-windows-msvc/release/dsh-tauri-app.exe
          if-no-files-found: error

  # Windows ARM64：交叉编译 aarch64-pc-windows-msvc。
  # Node v24.15.0 起官方提供 win-arm64 二进制（win-arm64/node.exe，已验证 200 OK）。
  # 实验性：payload 中 sharp 等原生模块仍为 x64 prebuild，ARM64 设备上靠
  # Windows 11 x64 模拟（Prism）或自愈链兜底，故 continue-on-error。
  build-windows-arm64:
    if: false  # dual-track: 本机只需 Windows x64，短路省 CI 分钟
    name: Windows ARM64 (NSIS, experimental)
    runs-on: windows-latest
    timeout-minutes: 90
    continue-on-error: true
    steps:
      - name: Resolve version and ref
        # 同 build-windows：dispatch 强制 checkout tag（版本混装防线①）
        id: ver
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
            echo "ref=refs/tags/$tag" >> "$GITHUB_OUTPUT"
          else
            tag='${{ github.ref_name }}'
            echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.ver.outputs.ref }}

      - name: Assert tauri.conf.json version == tag version
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED='${{ steps.ver.outputs.version }}'
          ACTUAL=$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' dsh-tauri/src-tauri/src/app/tauri.conf.json | head -1)
          if [ -z "$ACTUAL" ]; then
            echo "::error::无法从 dsh-tauri/src-tauri/src/app/tauri.conf.json 解析 version 字段"
            exit 1
          fi
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::版本混装防线：期望 ${EXPECTED}，但检出的 tauri.conf.json version=${ACTUAL}"
            exit 1
          fi
          echo "OK: tauri.conf.json version=${ACTUAL} == 期望 ${EXPECTED}"

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-pc-windows-msvc

      - name: Rust cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            dsh-tauri/src-tauri/target
          key: rust-win-arm64-${{ hashFiles('dsh-tauri/src-tauri/Cargo.lock') }}
          restore-keys: rust-win-arm64-

      - name: Install dsh-desktop dependencies
        run: cd dsh-desktop && npm ci --ignore-scripts
        shell: pwsh

      - name: Download vendor node (win-arm64)
        run: |
          mkdir -Force dsh-desktop\vendor\node | Out-Null
          Invoke-WebRequest -Uri 'https://nodejs.org/dist/v24.15.0/win-arm64/node.exe' -OutFile dsh-desktop\vendor\node\node.exe
        shell: pwsh

      - name: Download vendor npm
        run: |
          $tmp = "$env:TEMP\npm-pkg"
          mkdir -Force $tmp | Out-Null
          Invoke-WebRequest -Uri 'https://registry.npmmirror.com/npm/-/npm-10.9.2.tgz' -OutFile "$tmp\npm.tgz"
          tar -xzf "$tmp\npm.tgz" -C $tmp
          mkdir -Force dsh-desktop\vendor\npm | Out-Null
          Copy-Item "$tmp\package\*" dsh-desktop\vendor\npm\ -Recurse -Force
        shell: pwsh

      - name: Download rc7 client packages (for compat)
        run: |
          set -e
          cd dsh-desktop
          rc7_root="dist/win-unpacked/resources/app/node_modules"
          mkdir -p "$rc7_root"
          # PD1 对账（v0.5.2 复盘）：6 个 rc.7 客户端包之外，build-client-compat.mjs
          # 的闭包 BFS 还需要 web-react 的 use-sync-external-store@1.2.0 与
          # schema-form 的 @deepseek-ai/schemastery@3.18.1——v0.5.2 只下 6 个，
          # compat 构建抛「闭包包缺失」被非阻断 WARN 吞掉 → 官方包缺
          # client-compat.js（插件端 missed the module table）。闭包全集 10 包
          # （+ schemastery 的 @standard-schema/spec@1.1.0 / @deepseek-ai/cosmokit@1.8.2）。
          for spec in "@deepseek-ai/dsh-client-web-react@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-schema-form@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-primitives@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-slots@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-attachment@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-renderer@0.1.0-rc.7" \
                      "use-sync-external-store@1.2.0" \
                      "@deepseek-ai/schemastery@3.18.1" \
                      "@standard-schema/spec@1.1.0" \
                      "@deepseek-ai/cosmokit@1.8.2"; do
            name="${spec%@*}"
            echo "Downloading $spec ..."
            tgz=$(npm pack "$spec" 2>&1 | tail -1) || { echo "WARN: $spec not found, skipping"; continue; }
            if [ ! -f "$tgz" ]; then echo "WARN: $spec tarball not found ($tgz), skipping"; continue; fi
            tmp=$(mktemp -d)
            tar -xzf "$tgz" -C "$tmp"
            mkdir -p "$rc7_root/$name"
            cp -r "$tmp/package/"* "$rc7_root/$name/"
            rm -f "$tgz"
            echo "  OK: $name"
          done
          echo "rc7 packages ready (6 client pkgs + 4 closure deps)"
        shell: bash

      - name: Stage payload (CI simplified)
        run: |
          set -e
          DEST="dsh-tauri/package-payload/dsh-desktop"
          mkdir -p "$DEST"
          cp dsh-desktop/*.js "$DEST/" 2>/dev/null || true
          cp dsh-desktop/package.json "$DEST/"
          cp -r dsh-desktop/scripts "$DEST/scripts"
          cp -r dsh-desktop/assets "$DEST/assets"
          mkdir -p "$DEST/vendor"
          cp -r dsh-desktop/vendor/node "$DEST/vendor/node"
          cp -r dsh-desktop/vendor/npm "$DEST/vendor/npm" 2>/dev/null || true
          # vendored 内核 tarball（随仓库提交）：0.5.7 起 boot 链 compat-pin 步骤
          # fail-closed 校验 vendor/dsh-kernel 与 kernel-pin 一致，缺目录即拒绝启动
          # （0.5.7 本地打包踩坑复盘：五平台 stage 清单均已补齐，勿再删）。
          cp -r dsh-desktop/vendor/dsh-kernel "$DEST/vendor/dsh-kernel"
          rm -rf "$DEST/node_modules"
          cp -r dsh-desktop/node_modules "$DEST/node_modules"
          rm -rf "$DEST/node_modules/electron" "$DEST/node_modules/electron-builder" "$DEST/node_modules/electron-winstaller" "$DEST/node_modules/@electron" "$DEST/node_modules/@img/sharp-darwin-arm64" "$DEST/node_modules/@img/sharp-libvips-darwin-arm64" "$DEST/node_modules/@img/sharp-wasm32" "$DEST/node_modules/@koromix/koffi-darwin-arm64"
          # Codex/Claude 原生二进制（optional platform packages，~700MB 死重）：内核子代理
          # 适配器仅在真正拉起 Codex/Claude Code 进程时才解析这些 optional 二进制；import
          # 只读 JS 侧 metapackage。glob 精准命中各平台 variant（codex-win32-x64 /
          # codex-linux-x64 / claude-agent-sdk-darwin-arm64 ...），保留 metapackage 不断 import 图。
          rm -rf "$DEST"/node_modules/@openai/codex-* "$DEST"/node_modules/@anthropic-ai/claude-agent-sdk-*
          # rc7 vendor（非阻断——renderer 三级回落链兜底）
          if [ -d "dsh-desktop/dist/win-unpacked/resources/app/node_modules/@deepseek-ai" ]; then
            for pkg in dsh-desktop/dist/win-unpacked/resources/app/node_modules/@deepseek-ai/*/; do
              name=$(basename "$pkg")
              [ -d "$DEST/node_modules/@deepseek-ai/$name" ] || cp -r "$pkg" "$DEST/node_modules/@deepseek-ai/$name"
            done
          fi
          # compat（构建报错可不阻断——但产物必须在位，见下方 PD1 门禁）
          node dsh-tauri/scripts/build-client-compat.mjs 2>&1 || echo "WARN: compat failed (non-blocking)"
          # PD1 复盘门禁：v0.5.2 的 compat 闭包缺包被上一行 WARN 静默吞掉，官方
          # 包缺 client-compat.js（插件端 missed the module table 常态化）。
          # 构建器允许降级告警，装出去的包不允许缺件——缺即拒绝发布。
          COMPAT="$DEST/node_modules/@deepseek-ai/dsh-web-frontend/dist/assets/client-compat.js"
          if [ ! -f "$COMPAT" ]; then
            echo "::error::client-compat.js 缺失（build-client-compat 闭包失败——rc7 下载缺包？）"
            exit 1
          fi
          echo "OK: client-compat.js in payload ($(stat -c%s "$COMPAT") bytes)"
          echo "Payload: $(du -sm "$DEST" | cut -f1)MB"
        shell: bash

      - name: Build installer (aarch64 cross)
        run: |
          cd dsh-tauri
          npx --yes @tauri-apps/cli@latest build --config src-tauri/src/app/tauri.conf.json --target aarch64-pc-windows-msvc 2>&1 || {
            echo "=== ARM64 BUILD FAILED（实验性，不阻断发布） ==="
            exit 1
          }
        shell: bash

      - name: Rename installer (assert version + size)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          NSIS_DIR="dsh-tauri/src-tauri/target/aarch64-pc-windows-msvc/release/bundle/nsis"
          if [ ! -d "$NSIS_DIR" ]; then echo "no nsis bundle dir (build failed?), skip rename"; exit 0; fi
          cd "$NSIS_DIR"
          ls -la
          # 版本混装防线②：cache 恢复的 bundle 目录可能残留上个版本的改名产物
          #（上传 glob 是 DSH-Desktop-Setup-*，非本版本旧文件必须清掉）。
          find . -maxdepth 1 -name 'DSH-Desktop-Setup-*' ! -name "DSH-Desktop-Setup-${VERSION}-win-arm64.exe" -delete
          shopt -s nullglob
          src=( *_"${VERSION}"_arm64-setup.exe )
          if [ ${#src[@]} -eq 0 ]; then
            echo "::error::未找到 *_${VERSION}_arm64-setup.exe——NSIS 内嵌版本与期望不符（实验线也不允许版本混装；见上方目录清单）"
            exit 1
          fi
          cp "${src[0]}" "DSH-Desktop-Setup-${VERSION}-win-arm64.exe"
          size=$(stat -c%s "DSH-Desktop-Setup-${VERSION}-win-arm64.exe")
          if [ "$size" -lt 52428800 ]; then
            echo "::error::安装包体积异常（${size} 字节 < 50MB；v0.5.0 参考：~68MB）"
            exit 1
          fi
          echo "OK: DSH-Desktop-Setup-${VERSION}-win-arm64.exe ($size bytes)"

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: win-arm64-setup
          path: dsh-tauri/src-tauri/target/aarch64-pc-windows-msvc/release/bundle/nsis/DSH-Desktop-Setup-*-win-arm64.exe
          if-no-files-found: ignore

  # Windows x64 便携版 zip：解压到任意目录即用，数据存 exe 旁 data/。
  # exe 来自 build-windows 产物（win-x64-bin），resources 与安装包同源重新组装。
  build-portable:
    name: Windows x64 Portable (zip)
    runs-on: windows-latest
    timeout-minutes: 45
    needs: [build-windows]
    if: needs.build-windows.result == 'success'
    steps:
      - name: Resolve version and ref
        # 同 build-windows：dispatch 强制 checkout tag——便携版 resources（scripts/
        # sidecar/ui/payload 组装脚本）必须与安装包同源同版本。
        id: ver
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
            echo "ref=refs/tags/$tag" >> "$GITHUB_OUTPUT"
          else
            tag='${{ github.ref_name }}'
            echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.ver.outputs.ref }}

      - name: Assert tauri.conf.json version == tag version
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED='${{ steps.ver.outputs.version }}'
          ACTUAL=$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' dsh-tauri/src-tauri/src/app/tauri.conf.json | head -1)
          if [ -z "$ACTUAL" ]; then
            echo "::error::无法从 dsh-tauri/src-tauri/src/app/tauri.conf.json 解析 version 字段"
            exit 1
          fi
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::版本混装防线：期望 ${EXPECTED}，但检出的 tauri.conf.json version=${ACTUAL}"
            exit 1
          fi
          echo "OK: tauri.conf.json version=${ACTUAL} == 期望 ${EXPECTED}"

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Download exe artifact
        uses: actions/download-artifact@v4
        with:
          name: win-x64-bin
          path: exe-artifact

      # ---- 以下与 build-windows 的 payload 组装完全同源（便携版 resources
      #      必须与安装包逐字节同源，否则装出来的包与 zip 行为分叉）----
      - name: Install dsh-desktop dependencies
        run: cd dsh-desktop && npm ci --ignore-scripts
        shell: pwsh

      - name: Download vendor node
        run: |
          mkdir -Force dsh-desktop\vendor\node | Out-Null
          Invoke-WebRequest -Uri 'https://nodejs.org/dist/v24.15.0/win-x64/node.exe' -OutFile dsh-desktop\vendor\node\node.exe
        shell: pwsh

      - name: Download vendor npm
        run: |
          $tmp = "$env:TEMP\npm-pkg"
          mkdir -Force $tmp | Out-Null
          Invoke-WebRequest -Uri 'https://registry.npmmirror.com/npm/-/npm-10.9.2.tgz' -OutFile "$tmp\npm.tgz"
          tar -xzf "$tmp\npm.tgz" -C $tmp
          mkdir -Force dsh-desktop\vendor\npm | Out-Null
          Copy-Item "$tmp\package\*" dsh-desktop\vendor\npm\ -Recurse -Force
        shell: pwsh

      - name: Download rc7 client packages (for compat)
        run: |
          set -e
          cd dsh-desktop
          rc7_root="dist/win-unpacked/resources/app/node_modules"
          mkdir -p "$rc7_root"
          # PD1 对账（v0.5.2 复盘）：6 个 rc.7 客户端包之外，build-client-compat.mjs
          # 的闭包 BFS 还需要 web-react 的 use-sync-external-store@1.2.0 与
          # schema-form 的 @deepseek-ai/schemastery@3.18.1——v0.5.2 只下 6 个，
          # compat 构建抛「闭包包缺失」被非阻断 WARN 吞掉 → 官方包缺
          # client-compat.js（插件端 missed the module table）。闭包全集 10 包
          # （+ schemastery 的 @standard-schema/spec@1.1.0 / @deepseek-ai/cosmokit@1.8.2）。
          for spec in "@deepseek-ai/dsh-client-web-react@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-schema-form@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-primitives@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-slots@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-attachment@0.1.0-rc.7" \
                      "@deepseek-ai/dsh-client-ui-renderer@0.1.0-rc.7" \
                      "use-sync-external-store@1.2.0" \
                      "@deepseek-ai/schemastery@3.18.1" \
                      "@standard-schema/spec@1.1.0" \
                      "@deepseek-ai/cosmokit@1.8.2"; do
            name="${spec%@*}"
            echo "Downloading $spec ..."
            tgz=$(npm pack "$spec" 2>&1 | tail -1) || { echo "WARN: $spec not found, skipping"; continue; }
            if [ ! -f "$tgz" ]; then echo "WARN: $spec tarball not found ($tgz), skipping"; continue; fi
            tmp=$(mktemp -d)
            tar -xzf "$tgz" -C "$tmp"
            mkdir -p "$rc7_root/$name"
            cp -r "$tmp/package/"* "$rc7_root/$name/"
            rm -f "$tgz"
            echo "  OK: $name"
          done
          echo "rc7 packages ready (6 client pkgs + 4 closure deps)"
        shell: bash

      - name: Stage payload (CI simplified)
        run: |
          set -e
          DEST="dsh-tauri/package-payload/dsh-desktop"
          mkdir -p "$DEST"
          cp dsh-desktop/*.js "$DEST/" 2>/dev/null || true
          cp dsh-desktop/package.json "$DEST/"
          cp -r dsh-desktop/scripts "$DEST/scripts"
          cp -r dsh-desktop/assets "$DEST/assets"
          mkdir -p "$DEST/vendor"
          cp -r dsh-desktop/vendor/node "$DEST/vendor/node"
          cp -r dsh-desktop/vendor/npm "$DEST/vendor/npm" 2>/dev/null || true
          # vendored 内核 tarball（随仓库提交）：0.5.7 起 boot 链 compat-pin 步骤
          # fail-closed 校验 vendor/dsh-kernel 与 kernel-pin 一致，缺目录即拒绝启动
          # （0.5.7 本地打包踩坑复盘：五平台 stage 清单均已补齐，勿再删）。
          cp -r dsh-desktop/vendor/dsh-kernel "$DEST/vendor/dsh-kernel"
          rm -rf "$DEST/node_modules"
          cp -r dsh-desktop/node_modules "$DEST/node_modules"
          rm -rf "$DEST/node_modules/electron" "$DEST/node_modules/electron-builder" "$DEST/node_modules/electron-winstaller" "$DEST/node_modules/@electron" "$DEST/node_modules/@img/sharp-darwin-arm64" "$DEST/node_modules/@img/sharp-libvips-darwin-arm64" "$DEST/node_modules/@img/sharp-wasm32" "$DEST/node_modules/@koromix/koffi-darwin-arm64"
          # Codex/Claude 原生二进制（optional platform packages，~700MB 死重）：内核子代理
          # 适配器仅在真正拉起 Codex/Claude Code 进程时才解析这些 optional 二进制；import
          # 只读 JS 侧 metapackage。glob 精准命中各平台 variant（codex-win32-x64 /
          # codex-linux-x64 / claude-agent-sdk-darwin-arm64 ...），保留 metapackage 不断 import 图。
          rm -rf "$DEST"/node_modules/@openai/codex-* "$DEST"/node_modules/@anthropic-ai/claude-agent-sdk-*
          # rc7 vendor（非阻断——renderer 三级回落链兜底）
          if [ -d "dsh-desktop/dist/win-unpacked/resources/app/node_modules/@deepseek-ai" ]; then
            for pkg in dsh-desktop/dist/win-unpacked/resources/app/node_modules/@deepseek-ai/*/; do
              name=$(basename "$pkg")
              [ -d "$DEST/node_modules/@deepseek-ai/$name" ] || cp -r "$pkg" "$DEST/node_modules/@deepseek-ai/$name"
            done
          fi
          # compat（构建报错可不阻断——但产物必须在位，见下方 PD1 门禁）
          node dsh-tauri/scripts/build-client-compat.mjs 2>&1 || echo "WARN: compat failed (non-blocking)"
          # PD1 复盘门禁：v0.5.2 的 compat 闭包缺包被上一行 WARN 静默吞掉，官方
          # 包缺 client-compat.js（插件端 missed the module table 常态化）。
          # 构建器允许降级告警，装出去的包不允许缺件——缺即拒绝发布。
          COMPAT="$DEST/node_modules/@deepseek-ai/dsh-web-frontend/dist/assets/client-compat.js"
          if [ ! -f "$COMPAT" ]; then
            echo "::error::client-compat.js 缺失（build-client-compat 闭包失败——rc7 下载缺包？）"
            exit 1
          fi
          echo "OK: client-compat.js in payload ($(stat -c%s "$COMPAT") bytes)"
          echo "Payload: $(du -sm "$DEST" | cut -f1)MB"
        shell: bash

      # ---- 组装便携版布局（与 NSIS 安装后布局一致）----
      #   DSH Desktop/DSH Desktop.exe        ← win-x64-bin 产物
      #   DSH Desktop/portable.marker        ← 便携模式标记（userData → exe 旁 data/）
      #   DSH Desktop/resources/dsh-desktop|sidecar|ui/
      - name: Assemble portable layout
        shell: bash
        run: |
          ROOT="portable/DSH Desktop"
          mkdir -p "$ROOT/resources"
          cp exe-artifact/dsh-tauri-app.exe "$ROOT/DSH Desktop.exe"
          printf 'DSH Desktop portable marker\n%s\n' "存在此文件时 DSH Desktop 以便携模式运行：用户数据保存在本目录 data/ 下。删除此文件则恢复常规安装版数据路径。" > "$ROOT/portable.marker"
          # robocopy 成功码 0-7，≥8 才失败（Git Bash 下 flag 用 // 形式）
          rc=0; robocopy "dsh-tauri/package-payload/dsh-desktop" "$ROOT/resources/dsh-desktop" //MIR //R:2 //W:1 > /dev/null || rc=$?
          [ "$rc" -lt 8 ] || { echo "robocopy dsh-desktop failed: $rc"; exit 1; }
          # Windows 包不带 unix node 二进制（stage-payload 口径；防本机历史残留混入）
          rm -f "$ROOT/resources/dsh-desktop/vendor/node/node"
          # N1 门禁（v0.5.4 便携版修复）：win node.exe 应在位——三级解析链
          # （系统 ≥22 → 内置 vendor → 清晰报错）的保底层。缺件会让无本地
          # node 的便携版用户只剩报错路径（历史版本实测「打不开」主因之一）。
          # 降级为告警（非 exit 1）：下载/staging 步骤已成功、node.exe 通常
          # 在位，robocopy 后路径偶有平台差异致误判——三级链系统 node 兜底，
          # 不阻断发布。
          if [ -f "$ROOT/resources/dsh-desktop/vendor/node/node.exe" ]; then
            echo "OK: 内置 node.exe 在位 ($(stat -c%s "$ROOT/resources/dsh-desktop/vendor/node/node.exe") bytes)"
          else
            echo "::warning::便携版缺内置 node.exe（vendor/node/node.exe）——三级解析链将回落系统 node；无本地 node 的便携版用户会走到清晰报错路径"
            ls -la "$ROOT/resources/dsh-desktop/vendor/node" 2>&1 | head -5 || true
          fi
          rc=0; robocopy "dsh-tauri/sidecar" "$ROOT/resources/sidecar" //MIR //R:2 //W:1 > /dev/null || rc=$?
          [ "$rc" -lt 8 ] || { echo "robocopy sidecar failed: $rc"; exit 1; }
          rc=0; robocopy "dsh-tauri/ui" "$ROOT/resources/ui" //MIR //R:2 //W:1 > /dev/null || rc=$?
          [ "$rc" -lt 8 ] || { echo "robocopy ui failed: $rc"; exit 1; }
          echo "=== 便携版布局 ==="
          ls -la "$ROOT"
          du -sm "$ROOT" "$ROOT/resources/dsh-desktop"

      - name: Bundle D3DCOMPILER_47.dll beside portable exe (B1 fix)
        # B1 修复：便携版布局手工组装，不经 NSIS/tauri resources，需显式把 DLL
        # 放到 exe 同目录（与安装版 $INSTDIR 根旁路口径一致），否则精简系统用户
        # 解压即撞「丢失 D3DCOMPILER_47.dll」。
        shell: bash
        run: |
          set -euo pipefail
          ROOT="portable/DSH Desktop"
          [ -f "dsh-tauri/dlls/D3DCOMPILER_47.dll" ] || { echo "::error::缺少 dsh-tauri/dlls/D3DCOMPILER_47.dll"; exit 1; }
          cp "dsh-tauri/dlls/D3DCOMPILER_47.dll" "$ROOT/D3DCOMPILER_47.dll"
          [ -f "$ROOT/D3DCOMPILER_47.dll" ] && [ -f "$ROOT/DSH Desktop.exe" ] || { echo "::error::便携版 B1 布局异常"; exit 1; }
          echo "OK: $ROOT/D3DCOMPILER_47.dll ($(stat -c%s "$ROOT/D3DCOMPILER_47.dll") bytes)"

      - name: Create portable zip
        shell: pwsh
        run: |
          $VERSION = "${{ steps.ver.outputs.version }}"
          Compress-Archive -Path "portable/DSH Desktop" -DestinationPath "DSH-Desktop-Portable-$VERSION-win-x64.zip" -Force
          $mb = [math]::Round((Get-Item "DSH-Desktop-Portable-$VERSION-win-x64.zip").Length / 1MB, 1)
          Write-Host "portable zip: DSH-Desktop-Portable-$VERSION-win-x64.zip ($mb MB)"

      - name: Verify portable zip (version + size)
        # 版本混装防线③：zip 内 exe 来自 build-windows（其 NSIS 名已断言版本），
        # 此处断言 zip 本身存在、文件名版本正确、体积 >50MB（v0.5.0 参考：~102MB）。
        shell: bash
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          f="DSH-Desktop-Portable-${VERSION}-win-x64.zip"
          [ -f "$f" ] || { echo "::error::便携版 zip 缺失: $f"; exit 1; }
          size=$(stat -c%s "$f")
          if [ "$size" -lt 52428800 ]; then
            echo "::error::便携版体积异常（${size} 字节 < 50MB）——疑似 resources 组装缺失"
            exit 1
          fi
          echo "OK: $f ($size bytes)"

      - name: Assemble portable lite layout (no bundled node)
        # LITE 变体：删 vendor/node/node.exe——机器上已有 node 的用户选它，
        # 体积更小；与带 node 的主便携 zip 共存发布。
        shell: pwsh
        run: |
          $VERSION = "${{ steps.ver.outputs.version }}"
          if (Test-Path "portable-lite") { Remove-Item "portable-lite" -Recurse -Force }
          New-Item -ItemType Directory -Force -Path "portable-lite" | Out-Null
          Copy-Item "portable/DSH Desktop" "portable-lite/DSH Desktop" -Recurse -Force
          $nodeExe = "portable-lite/DSH Desktop/resources/dsh-desktop/vendor/node/node.exe"
          if (Test-Path $nodeExe) { Remove-Item $nodeExe -Force }
          Compress-Archive -Path "portable-lite/DSH Desktop" -DestinationPath "DSH-Desktop-Portable-$VERSION-win-x64-lite.zip" -Force
          $mb = [math]::Round((Get-Item "DSH-Desktop-Portable-$VERSION-win-x64-lite.zip").Length / 1MB, 1)
          Write-Host "portable lite zip: DSH-Desktop-Portable-$VERSION-win-x64-lite.zip ($mb MB)"

      - name: Verify portable lite zip (version + size)
        shell: bash
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          f="DSH-Desktop-Portable-${VERSION}-win-x64-lite.zip"
          [ -f "$f" ] || { echo "::error::便携 lite zip 缺失: $f"; exit 1; }
          size=$(stat -c%s "$f")
          if [ "$size" -lt 52428800 ]; then
            echo "::error::便携 lite zip 体积异常（${size} 字节 < 50MB）"
            exit 1
          fi
          echo "OK: $f ($size bytes)"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: win-x64-portable
          path: |
            DSH-Desktop-Portable-*-win-x64.zip
            DSH-Desktop-Portable-*-win-x64-lite.zip
          if-no-files-found: error

  build-linux:
    if: false  # dual-track: 本机只需 Windows x64，短路省 CI 分钟
    name: Linux x64 (AppImage + deb)
    runs-on: ubuntu-22.04
    timeout-minutes: 75
    continue-on-error: true
    steps:
      - name: Resolve version and ref
        # 同 build-windows：dispatch 强制 checkout tag（版本混装防线①）
        id: ver
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
            echo "ref=refs/tags/$tag" >> "$GITHUB_OUTPUT"
          else
            tag='${{ github.ref_name }}'
            echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.ver.outputs.ref }}

      - name: Assert tauri.conf.json version == tag version
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED='${{ steps.ver.outputs.version }}'
          ACTUAL=$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' dsh-tauri/src-tauri/src/app/tauri.conf.json | head -1)
          if [ -z "$ACTUAL" ]; then
            echo "::error::无法从 dsh-tauri/src-tauri/src/app/tauri.conf.json 解析 version 字段"
            exit 1
          fi
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::版本混装防线：期望 ${EXPECTED}，但检出的 tauri.conf.json version=${ACTUAL}"
            exit 1
          fi
          echo "OK: tauri.conf.json version=${ACTUAL} == 期望 ${EXPECTED}"

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install system deps
        run: |
          sudo apt-get update -qq
          # libfuse2：linuxdeploy(AppImage) 运行依赖——ubuntu-22.04 默认不带，
          # 缺它 AppImage 打包报 failed to run linuxdeploy
          sudo apt-get install -y -qq libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libfuse2

      - name: Rust cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            dsh-tauri/src-tauri/target
          key: rust-linux-${{ hashFiles('dsh-tauri/src-tauri/Cargo.lock') }}

      - name: Install dsh-desktop dependencies
        run: cd dsh-desktop && npm ci --ignore-scripts

      - name: Download vendor node
        run: |
          mkdir -p dsh-desktop/vendor/node
          wget -q https://nodejs.org/dist/v24.15.0/node-v24.15.0-linux-x64.tar.gz
          tar -xzf node-v24.15.0-linux-x64.tar.gz
          cp node-v24.15.0-linux-x64/bin/node dsh-desktop/vendor/node/node
          chmod +x dsh-desktop/vendor/node/node

      - name: Download rc7 client packages
        run: |
          cd dsh-desktop
          dest="dist/win-unpacked/resources/app/node_modules/@deepseek-ai"
          mkdir -p "$dest"
          for pkg in dsh-client-web-react dsh-client-schema-form dsh-client-ui-primitives dsh-client-ui-slots dsh-client-ui-attachment dsh-client-ui-renderer; do
            full="@deepseek-ai/$pkg"
            echo "Downloading $full@0.1.0-rc.7 ..."
            tgz=$(npm pack "$full@0.1.0-rc.7" 2>&1 | tail -1)
            if [ ! -f "$tgz" ]; then echo "WARN: $full not found, skipping"; continue; fi
            tmp=$(mktemp -d)
            tar -xzf "$tgz" -C "$tmp"
            mkdir -p "$dest/$pkg"
            cp -r "$tmp/package/"* "$dest/$pkg/"
            rm -f "$tgz"
          done
          echo "rc7 packages ready"

      - name: Stage payload (CI simplified)
        run: |
          set -e
          DEST="dsh-tauri/package-payload/dsh-desktop"
          mkdir -p "$DEST"
          cp dsh-desktop/*.js "$DEST/" 2>/dev/null || true
          cp dsh-desktop/package.json "$DEST/"
          cp -r dsh-desktop/scripts "$DEST/scripts"
          cp -r dsh-desktop/assets "$DEST/assets"
          mkdir -p "$DEST/vendor"
          cp -r dsh-desktop/vendor/node "$DEST/vendor/node"
          cp -r dsh-desktop/vendor/npm "$DEST/vendor/npm" 2>/dev/null || true
          # vendored 内核 tarball（随仓库提交）：0.5.7 起 boot 链 compat-pin 步骤
          # fail-closed 校验 vendor/dsh-kernel 与 kernel-pin 一致，缺目录即拒绝启动
          # （0.5.7 本地打包踩坑复盘：五平台 stage 清单均已补齐，勿再删）。
          cp -r dsh-desktop/vendor/dsh-kernel "$DEST/vendor/dsh-kernel"
          rm -rf "$DEST/node_modules"
          cp -r dsh-desktop/node_modules "$DEST/node_modules"
          rm -rf "$DEST/node_modules/electron" "$DEST/node_modules/electron-builder" "$DEST/node_modules/electron-winstaller" "$DEST/node_modules/@electron"
          # Codex/Claude 原生二进制（optional platform packages，~700MB 死重）：内核子代理
          # 适配器仅在真正拉起 Codex/Claude Code 进程时才解析这些 optional 二进制；import
          # 只读 JS 侧 metapackage。glob 精准命中各平台 variant（codex-win32-x64 /
          # codex-linux-x64 / claude-agent-sdk-darwin-arm64 ...），保留 metapackage 不断 import 图。
          rm -rf "$DEST"/node_modules/@openai/codex-* "$DEST"/node_modules/@anthropic-ai/claude-agent-sdk-*
          # musl 产物全剔除：linuxdeploy 扫描 AppDir 内全部 ELF，遇 musl 链接
          # 的原生模块（@photostructure/sqlite 的 *.musl.node、@koromix/koffi
          # 的 musl_x64/ 目录等）会找 libc.musl-x86_64.so.1（glibc runner 上
          # 不存在）→ AppImage 打包失败。glibc 系统上 musl 产物全是死重量。
          find "$DEST/node_modules" "$DEST/assets" -iname "*musl*" -not -name "*.py" -exec rm -rf {} + 2>/dev/null || true
          echo "Payload: $(du -sm "$DEST" | cut -f1)MB"

      - name: Build installer
        # --bundles 覆盖 tauri.conf.json 的 targets:["nsis"]（Windows 专用，
        # 在 Linux/macOS 上不会产出任何 bundle——历史 run 因此从未真正打过
        # Linux/macOS 安装包，旧 upload 用 warn 级别静默吞掉了）。
        # 第二个 --config 内联覆盖 productName 去空格："DSH Desktop"（含空格）
        # 是 linuxdeploy/AppImage 打包失败的已知根因（deb 不受影响）；产物
        # 最终文件名由 Rename 步骤统一，与 productName 无耦合。
        # deb 先行（必须成功）；AppImage 单独跑且非阻断——有 deb 兜底。
        # APPIMAGE_EXTRACT_AND_RUN=1：linuxdeploy 自身是 AppImage，runner 无
        # /dev/fuse 无法自挂载执行（exec 失败＝"failed to run linuxdeploy"），
        # 强制解压运行；NO_STRIP=1 防符号剥离报错。
        run: |
          cd dsh-tauri
          npx --yes @tauri-apps/cli@latest build --config src-tauri/src/app/tauri.conf.json --config '{"productName":"DSH-Desktop"}' --target x86_64-unknown-linux-gnu --bundles deb
          export APPIMAGE_EXTRACT_AND_RUN=1 NO_STRIP=1
          npx --yes @tauri-apps/cli@latest build --config src-tauri/src/app/tauri.conf.json --config '{"productName":"DSH-Desktop"}' --target x86_64-unknown-linux-gnu --bundles appimage || echo "::warning::AppImage bundling failed (deb still published)"

      - name: Rename artifacts (assert version + size)
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          BUNDLE=dsh-tauri/src-tauri/target/x86_64-unknown-linux-gnu/release/bundle
          ls -la "$BUNDLE/appimage" "$BUNDLE/deb" || true
          shopt -s nullglob
          # deb（底线产物）：产物名内嵌 tauri.conf.json 版本（<name>_<ver>_amd64.deb）
          debs=( "$BUNDLE"/deb/*_"${VERSION}"_amd64.deb )
          if [ ${#debs[@]} -eq 0 ]; then
            echo "::error::未找到 *_${VERSION}_amd64.deb——deb 内嵌版本与期望不符（见上方目录清单）"
            exit 1
          fi
          cp "${debs[0]}" "dsh-desktop_${VERSION}_amd64.deb"
          size=$(stat -c%s "dsh-desktop_${VERSION}_amd64.deb")
          if [ "$size" -lt 52428800 ]; then
            echo "::error::deb 体积异常（${size} 字节 < 50MB；v0.5.0 参考：~122MB）"
            exit 1
          fi
          echo "OK: dsh-desktop_${VERSION}_amd64.deb ($size bytes)"
          # AppImage（可选——linuxdeploy 偶发失败有 deb 兜底）：产出时同样断言版本
          appimages=( "$BUNDLE"/appimage/*_"${VERSION}"_*.AppImage )
          if [ ${#appimages[@]} -gt 0 ]; then
            cp "${appimages[0]}" "DSH-Desktop-${VERSION}-linux-x64.AppImage"
            asize=$(stat -c%s "DSH-Desktop-${VERSION}-linux-x64.AppImage")
            if [ "$asize" -lt 52428800 ]; then
              echo "::error::AppImage 体积异常（${asize} 字节 < 50MB；v0.5.0 参考：~190MB）"
              exit 1
            fi
            echo "OK: DSH-Desktop-${VERSION}-linux-x64.AppImage ($asize bytes)"
          else
            echo "::warning::AppImage 未产出（仅 deb 发布）"
          fi
          ls -la dsh-desktop_*_amd64.deb DSH-Desktop-*-linux-x64.AppImage 2>/dev/null || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-x64
          path: |
            DSH-Desktop-*-linux-x64.AppImage
            dsh-desktop_*_amd64.deb
          if-no-files-found: warn

  build-macos:
    if: false  # dual-track: 本机只需 Windows x64，短路省 CI 分钟
    name: macOS ARM64 (dmg)
    runs-on: macos-14
    timeout-minutes: 75
    continue-on-error: true
    steps:
      - name: Resolve version and ref
        # 同 build-windows：dispatch 强制 checkout tag（版本混装防线①）
        id: ver
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
            echo "ref=refs/tags/$tag" >> "$GITHUB_OUTPUT"
          else
            tag='${{ github.ref_name }}'
            echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.ver.outputs.ref }}

      - name: Assert tauri.conf.json version == tag version
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED='${{ steps.ver.outputs.version }}'
          ACTUAL=$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' dsh-tauri/src-tauri/src/app/tauri.conf.json | head -1)
          if [ -z "$ACTUAL" ]; then
            echo "::error::无法从 dsh-tauri/src-tauri/src/app/tauri.conf.json 解析 version 字段"
            exit 1
          fi
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::版本混装防线：期望 ${EXPECTED}，但检出的 tauri.conf.json version=${ACTUAL}"
            exit 1
          fi
          echo "OK: tauri.conf.json version=${ACTUAL} == 期望 ${EXPECTED}"

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Rust cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            dsh-tauri/src-tauri/target
          key: rust-mac-${{ hashFiles('dsh-tauri/src-tauri/Cargo.lock') }}

      - name: Install dsh-desktop dependencies
        run: cd dsh-desktop && npm ci --ignore-scripts

      - name: Download vendor node
        run: |
          mkdir -p dsh-desktop/vendor/node
          wget -q https://nodejs.org/dist/v24.15.0/node-v24.15.0-darwin-arm64.tar.gz
          tar -xzf node-v24.15.0-darwin-arm64.tar.gz
          cp node-v24.15.0-darwin-arm64/bin/node dsh-desktop/vendor/node/node
          chmod +x dsh-desktop/vendor/node/node

      - name: Download rc7 client packages
        run: |
          cd dsh-desktop
          dest="dist/win-unpacked/resources/app/node_modules/@deepseek-ai"
          mkdir -p "$dest"
          for pkg in dsh-client-web-react dsh-client-schema-form dsh-client-ui-primitives dsh-client-ui-slots dsh-client-ui-attachment dsh-client-ui-renderer; do
            full="@deepseek-ai/$pkg"
            echo "Downloading $full@0.1.0-rc.7 ..."
            tgz=$(npm pack "$full@0.1.0-rc.7" 2>&1 | tail -1)
            if [ ! -f "$tgz" ]; then echo "WARN: $full not found, skipping"; continue; fi
            tmp=$(mktemp -d)
            tar -xzf "$tgz" -C "$tmp"
            mkdir -p "$dest/$pkg"
            cp -r "$tmp/package/"* "$dest/$pkg/"
            rm -f "$tgz"
          done
          echo "rc7 packages ready"

      - name: Stage payload (CI simplified)
        run: |
          set -e
          DEST="dsh-tauri/package-payload/dsh-desktop"
          mkdir -p "$DEST"
          cp dsh-desktop/*.js "$DEST/" 2>/dev/null || true
          cp dsh-desktop/package.json "$DEST/"
          cp -r dsh-desktop/scripts "$DEST/scripts"
          cp -r dsh-desktop/assets "$DEST/assets"
          mkdir -p "$DEST/vendor"
          cp -r dsh-desktop/vendor/node "$DEST/vendor/node"
          cp -r dsh-desktop/vendor/npm "$DEST/vendor/npm" 2>/dev/null || true
          # vendored 内核 tarball（随仓库提交）：0.5.7 起 boot 链 compat-pin 步骤
          # fail-closed 校验 vendor/dsh-kernel 与 kernel-pin 一致，缺目录即拒绝启动
          # （0.5.7 本地打包踩坑复盘：五平台 stage 清单均已补齐，勿再删）。
          cp -r dsh-desktop/vendor/dsh-kernel "$DEST/vendor/dsh-kernel"
          rm -rf "$DEST/node_modules"
          cp -r dsh-desktop/node_modules "$DEST/node_modules"
          rm -rf "$DEST/node_modules/electron" "$DEST/node_modules/electron-builder" "$DEST/node_modules/electron-winstaller" "$DEST/node_modules/@electron"
          # Codex/Claude 原生二进制（optional platform packages，~700MB 死重）：内核子代理
          # 适配器仅在真正拉起 Codex/Claude Code 进程时才解析这些 optional 二进制；import
          # 只读 JS 侧 metapackage。glob 精准命中各平台 variant（codex-win32-x64 /
          # codex-linux-x64 / claude-agent-sdk-darwin-arm64 ...），保留 metapackage 不断 import 图。
          rm -rf "$DEST"/node_modules/@openai/codex-* "$DEST"/node_modules/@anthropic-ai/claude-agent-sdk-*
          echo "Payload: $(du -sm "$DEST" | cut -f1)MB"

      - name: Build installer
        # --bundles dmg：同 Linux——覆盖 targets:["nsis"]，否则 macOS 不出 dmg
        run: cd dsh-tauri && npx --yes @tauri-apps/cli@latest build --config src-tauri/src/app/tauri.conf.json --target aarch64-apple-darwin --bundles dmg

      - name: Verify ad-hoc signature (arm64 内核强制代码签名)
        # v0.5.0 事故复盘：Tauri v2 在未配置 signingIdentity 且无 APPLE_CERTIFICATE
        # 时【完全跳过】codesign——.app 无 bundle 级密封（_CodeSignature 缺失），
        # 仅主二进制带 rustc/ld 链接期 ad-hoc 签名。Sequoia 起 Gatekeeper 对
        # quarantine 的此类应用直接报「已损坏」，xattr -cr 救不回。
        # 修复：tauri.conf.json 已显式 signingIdentity:"-"（ad-hoc），由 Tauri
        # 在打 DMG 前按由内向外顺序正规签名；本步骤验证签名真实存在，失败即
        # 回退显式重签并重打 DMG——绝不再发出无签名 mac 包。
        run: |
          set -e
          # 注意：Tauri 打完 DMG 会「Cleaning」删掉 bundle/macos/ 下的 .app
          # （v0.5.1 首跑实测：验已被清理的路径必假失败）——改为挂载 DMG 验
          # 内层 app；失败才解包重签重打。
          DMG_DIR="dsh-tauri/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg"
          DMG="$(ls "$DMG_DIR"/*.dmg | head -1)"
          [ -f "$DMG" ] || { echo "::error::未找到 DMG 产物（构建失败？）"; ls -la "$DMG_DIR" || true; exit 1; }
          MNT="$(mktemp -d)/dshd"
          hdiutil attach "$DMG" -mountpoint "$MNT" -nobrowse -readonly -quiet
          APP="$MNT/DSH Desktop.app"
          echo "=== codesign -dv（DMG 内层）==="
          codesign -dv "$APP" 2>&1 || true
          if codesign --verify --deep --strict "$APP"; then
            echo "OK: DMG 内 app bundle 签名有效（ad-hoc 密封完整）"
          else
            echo "::warning::签名缺失/无效——解包重签并重打 DMG"
            WORK="$(mktemp -d)"
            cp -R "$APP" "$WORK/DSH Desktop.app"
            hdiutil detach "$MNT" -quiet || true
            xattr -cr "$WORK/DSH Desktop.app" || true
            codesign --force --deep --sign - "$WORK/DSH Desktop.app"
            codesign --verify --deep --strict "$WORK/DSH Desktop.app"
            rm -f "$DMG_DIR"/*.dmg
            hdiutil create -volname "DSH Desktop" -srcfolder "$WORK/DSH Desktop.app" -ov -format UDZO "$DMG_DIR/resigned.dmg"
            MNT="$(mktemp -d)/dshd2"
            hdiutil attach "$DMG_DIR/resigned.dmg" -mountpoint "$MNT" -nobrowse -readonly -quiet
            APP="$MNT/DSH Desktop.app"
          fi
          echo "=== spctl 评估（ad-hoc 必然 rejected，仅留痕）==="
          spctl -a -t execute "$APP" 2>&1 || true
          hdiutil detach "$MNT" -quiet || true

      - name: Rename artifacts (assert version + size)
        run: |
          set -euo pipefail
          VERSION=${{ steps.ver.outputs.version }}
          BUNDLE=dsh-tauri/src-tauri/target/aarch64-apple-darwin/release/bundle
          # 版本混装防线③（v0.5.0 事故的直接防线）：DMG 内 Info.plist 的
          # CFBundleShortVersionString 必须等于期望版本。.app 已被 Tauri 打
          # DMG 后清理——挂载 DMG 读内层 plist（与签名校验步骤同款姿势）。
          DMG="$(ls "$BUNDLE"/dmg/*.dmg | head -1)"
          [ -f "$DMG" ] || { echo "::error::未找到 DMG 产物"; exit 1; }
          MNT="$(mktemp -d)/dshd"
          hdiutil attach "$DMG" -mountpoint "$MNT" -nobrowse -readonly -quiet
          plist_ver=$(plutil -extract CFBundleShortVersionString raw "$MNT/DSH Desktop.app/Contents/Info.plist")
          hdiutil detach "$MNT" -quiet || true
          echo "Info.plist CFBundleShortVersionString = ${plist_ver}"
          if [ "$plist_ver" != "$VERSION" ]; then
            echo "::error::DMG 内 Info.plist 版本 ${plist_ver} != 期望 ${VERSION}（版本混装）"
            exit 1
          fi
          ls -la "$BUNDLE/dmg" || true
          shopt -s nullglob
          dmgs=( "$BUNDLE"/dmg/*_"${VERSION}"_*.dmg )
          # 签名回退路径重打的 DMG 文件名不带版本（resigned.dmg）——内容版本
          # 已由上方 Info.plist 断言覆盖，允许兜底匹配
          if [ ${#dmgs[@]} -eq 0 ] && [ -f "$BUNDLE/dmg/resigned.dmg" ]; then
            dmgs=( "$BUNDLE/dmg/resigned.dmg" )
          fi
          if [ ${#dmgs[@]} -eq 0 ]; then
            echo "::error::未找到 *_${VERSION}_*.dmg——DMG 文件名版本与期望不符（见上方目录清单）"
            exit 1
          fi
          cp "${dmgs[0]}" "DSH-Desktop-${VERSION}-macos-arm64.dmg"
          # BSD stat（macOS）无 -c——用 wc -c 全平台可移植（此行是 v0.5.1
          # 第三跑才暴露的陈年坑：此前从未活到这一步）。
          size=$(wc -c < "DSH-Desktop-${VERSION}-macos-arm64.dmg" | tr -d ' ')
          if [ "$size" -lt 52428800 ]; then
            echo "::error::DMG 体积异常（${size} 字节 < 50MB；v0.5.0 参考：~117MB）"
            exit 1
          fi
          echo "OK: DSH-Desktop-${VERSION}-macos-arm64.dmg ($size bytes)"
          ls -la DSH-Desktop-*-macos-arm64.dmg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: DSH-Desktop-*-macos-arm64.dmg
          if-no-files-found: warn

  publish:
    name: Publish Release
    needs: [build-windows, build-windows-arm64, build-portable, build-linux, build-macos]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: always() && !cancelled() && (needs.build-windows.result == 'success' || needs.build-portable.result == 'success' || needs.build-linux.result == 'success' || needs.build-macos.result == 'success' || needs.build-windows-arm64.result == 'success')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Collect installers
        run: |
          set -euo pipefail
          mkdir -p release-assets
          # 全部发布产物（含便携版 zip）；排除中间产物（win-x64-bin 里的裸 exe，
          # 它只是便携版 job 的原料，不进 Release）
          find artifacts -type f \( -name "*.exe" -o -name "*.zip" -o -name "*.AppImage" -o -name "*.deb" -o -name "*.dmg" \) ! -name "dsh-tauri-app.exe" -exec cp {} release-assets/ \; || true
          if [ -z "$(ls -A release-assets)" ]; then
            echo "::error::没有任何构建产物被收集（所有 build job 均失败？）——中止发布"
            exit 1
          fi

      - name: Generate sha256 sidecars
        # 自动更新强制校验链配套：客户端壳侧下载平台资产后强制校验同名
        # .sha256 边车（解析规则：首段 64 位小写 hex）。边车在 publish 统一
        # 生成而非各 build job 生成——单点产出保证「一个主资产恰好一个边车」，
        # 且 dispatch 补传/重建资产时边车必然随新内容重建。
        # 格式与 sha256sum 原生输出一致（<hex><两空格><文件名>），壳侧/verify
        # 脚本均只取首段 hex，文件名尾注向后兼容可省略。
        # 注：本步骤只在 ubuntu runner 上跑（sha256sum GNU 版），mac build job
        # 不涉及——规避 BSD 工具链坑（stat -c 前车之鉴）。
        run: |
          set -euo pipefail
          cd release-assets
          main_count=0
          for f in *; do
            case "$f" in *.sha256) continue ;; esac
            main_count=$((main_count + 1))
            sha256sum "$f" > "$f.sha256"
            hash=$(cut -d' ' -f1 "$f.sha256")
            if ! echo "$hash" | grep -Eq '^[0-9a-f]{64}$'; then
              echo "::error::边车格式异常（首段不是 64 位小写 hex）: $f.sha256"
              exit 1
            fi
            echo "边车: $f.sha256  hash=$hash"
          done
          side_count=$(find . -maxdepth 1 -type f -name '*.sha256' | wc -l)
          if [ "$side_count" -ne "$main_count" ]; then
            echo "::error::边车数量 ${side_count} != 主资产数量 ${main_count}"
            exit 1
          fi
          echo "OK: ${side_count}/${main_count} 个主资产均已生成 sha256 边车"

      - name: Resolve release tag/version
        id: ver
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
          else
            tag='${{ github.ref_name }}'
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - name: Verify assets (版本一致 + 体积下限 + 边车齐全)
        # 版本混装防线④（最后一道闸）：每个资产文件名必须内嵌期望版本号、
        # 体积必须 >50MB。任一不满足即拒绝发布——上游 job 有 continue-on-error，
        # 不能全信，publish 是资产出仓前的总闸。
        # 边车（*.sha256）例外分支：不适用 50MB 下限（本身就是百来字节），
        # 改为校验 ①主资产存在 ②内容首段 64 位小写 hex；文件名版本检查对
        # 边车同样生效（边车名=主资产名+后缀，天然含版本）。
        run: |
          set -euo pipefail
          VERSION='${{ steps.ver.outputs.version }}'
          echo "=== 产物清单（期望版本 ${VERSION}）==="
          fail=0
          for f in release-assets/*; do
            name=$(basename "$f")
            case "$name" in
              *.sha256)
                main="${name%.sha256}"
                if [ ! -f "release-assets/$main" ]; then
                  echo "::error::边车没有对应主资产: $name"; fail=1
                fi
                if ! head -c 200 "$f" | grep -Eq '^[0-9a-f]{64}([[:space:]]|$)'; then
                  echo "::error::边车格式坏（首段非 64 位小写 hex）: $name"; fail=1
                fi
                ;;
              esac
            case "$name" in
              *"$VERSION"*) ;;
              *) echo "::error::资产文件名未包含期望版本 ${VERSION}: $name（版本混装？拒绝发布）"; fail=1 ;;
            esac
            size=$(stat -c%s "$f")
            case "$name" in
              *.sha256) ;; # 边车不适用 50MB 下限
              *)
                if [ "$size" -lt 52428800 ]; then
                  echo "::error::资产体积异常（<50MB）: $name（${size} 字节）"
                  fail=1
                fi ;;
            esac
            printf '%-52s %12d bytes\n' "$name" "$size"
          done
          main_n=$(find release-assets -maxdepth 1 -type f ! -name '*.sha256' | wc -l)
          side_n=$(find release-assets -maxdepth 1 -type f -name '*.sha256' | wc -l)
          if [ "$main_n" -ne "$side_n" ]; then
            echo "::error::边车数量 ${side_n} != 主资产数量 ${main_n}——每个主资产必须有边车（自动更新校验链）"
            fail=1
          fi
          [ "$fail" -eq 0 ] || exit 1
          echo "OK: ${main_n} 个主资产 + ${side_n} 个边车全部通过版本/体积/边车校验"

      - name: Generate release notes
        run: |
          TAG='${{ steps.ver.outputs.tag }}'
          VERSION='${{ steps.ver.outputs.version }}'
          cat > release-notes.md << EOF
          # DSH Desktop ${VERSION} — 全平台

          > 覆盖 v0.6.1 + v0.6.2：① 单项根治「多模态模型说我发不了图片」；② 内核输入区
          > （composer）换代漂移整族修复——9 个插件静默失效全部迁移修复；③ 插件同步
          > 把残留依赖树整树分发导致的卡死根治；④ 历史对话完整回溯与「吞消息」根治。

          ## ✨ 新功能

          **模型卡「支持图片输入」勾选**
          设置 → 模型管理，每个模型一行一个勾选框：勾上 = 显式声明支持图片，发图不再被
          「当前模型不支持图片」拒绝；取消 = 恢复纯文本。适合「模型本身支持视觉、但接口
          没声明」的场景（第三方中转 / 开源模型常见）。从「获取模型」目录挑选模型时会
          自动带上接口自报的图片能力。视频暂不提供勾选：当前内核协议只支持 text / image。

          **历史对话完整回溯（v0.6.1）**
          - 历史加载每页 50 → 200 条；滚到聊天顶部自动翻页加载更早记录；
          - 根治「历史只显示约三分之一」「翻到头早期对话仍缺失」——跨页断头自动续读。

          **「编辑并回退」重做（v0.6.2）**
          消息悬停「编辑并回退」：从任意一条历史消息处分叉新会话、预填原文重发，旧会话
          原样保留。此功能在输入区换代后一度完全失效，本版修复并实测通过。

          ## 🛠 修复

          **「吞消息」根治（v0.6.1）**
          偶发「消息发出去了、界面却不显示」：现在遇到装配异常自动从持久化完整历史窗口
          安全重建，不再丢消息；极端情况输出 [dsh-assembly-resilience] 前缀日志可取证。

          **输入区换代适配（9 个插件静默失效全修，v0.6.2）**
          编辑并回退 / 输入历史 ↑ 回溯 / 拖拽文件到对话 / 粘贴图片发送 / 对话导航条 /
          对话界面增强 / 长消息折叠 / 峰谷价格卫士 / 识图插件草稿被覆盖——全部修复，
          每处均做了修复前后交叉验证。

          **配套插件同步卡死（v0.6.2）**
          同步会把开发机残留依赖树（上万个文件）整树拷贝、卡数分钟。现在只分发随包正件，
          同步从分钟级回到秒级。

          ## 安装包

          | 平台 | 格式 |
          |------|------|
          | Windows x64 | 安装包 DSH-Desktop-Setup-${VERSION}-win-x64.exe |
          | Windows x64 | 便携版 DSH-Desktop-Portable-${VERSION}-win-x64.zip（解压即用，数据存 exe 旁 data/ 目录） |
          | Windows ARM64 | 实验性安装包 DSH-Desktop-Setup-${VERSION}-win-arm64.exe |
          | Linux x64 | AppImage / .deb |
          | macOS ARM64 | .dmg |

          ## 升级说明

          1. 完全退出当前 DSH Desktop（托盘右键 → 退出）
          2. 下载对应平台安装包并运行（覆盖安装，装回原目录，数据不丢失）
          3. 首次启动自动对齐补丁与插件

          ## 详细更新日志

          见 [CHANGELOG.md](https://github.com/myYangyunfan/dsh_desktop/blob/main/dsh-tauri/CHANGELOG.md)
          EOF

      - name: Upload assets to Release
        # 上传纪律：
        #  1) release 不存在时才创建；已存在时绝不触碰 title/body/prerelease/draft
        #     ——此前 softprops/action-gh-release 每次运行都会把 Pre-release 和
        #     预览版标题冲掉（draft:false + prerelease:false 写死）。
        #  2) 同名资产先 delete-asset 再上传（而非 fail-fast）：「给已发布版本
        #     补传/重建资产」正是 workflow_dispatch 的第一用途，fail-fast 会让
        #     该用途无法执行；显式删除后重传是幂等重建，且下一步会逐个核对
        #     存在性与大小，不留旧内容残影、不会出现重复资产。
          #  3) 上传失败不吞错（旧版 || echo WARN 会静默丢资产）。
          #  4) 循环遍历 release-assets/* 全部文件——.sha256 边车与主资产
          #     同目录，天然走同一套 delete-then-upload 幂等路径（重跑 release
          #     job 时边车同样删后传，不会残留旧哈希）。
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # publish job 无 checkout（无 git 上下文）——gh 必须显式知道仓库，
          # 否则 create 走「从本地 git 推断」路径直接 fatal（v0.5.1 第三跑
          # 实证：六资产校验全过、create 崩在 not a git repository）。
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          TAG='${{ steps.ver.outputs.tag }}'
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            echo "release $TAG 不存在——创建（仅此一次触碰 release 元数据）"
            # 不带 --prerelease：更新器的 /releases/latest 端点跳过 prerelease，
            # 出生即 prerelease 意味着更新检查永远看不到新版本（直到手动翻
            # latest——v0.5.7 首发即踩，「自动更新一直不生效」的全局根因之一）。
            # 正式 release 自动成为 latest，更新链路即刻可见。
            gh release create "$TAG" --title "DSH Desktop $TAG" --notes-file release-notes.md
          fi
          for f in release-assets/*; do
            name=$(basename "$f")
            if gh release view "$TAG" --json assets --jq '.assets[].name' | grep -Fxq "$name"; then
              echo "删除已存在的同名资产: $name"
              gh release delete-asset "$TAG" "$name" --yes
            fi
            echo "上传: $f"
            gh release upload "$TAG" "$f"
          done

      - name: Verify uploaded assets
        # 上传后校验（资产去重纪律的收口）：每个资产都在 release 上存在、
        # 大小与本地一致且非零。另列出 release 全部资产供人工核对重复。
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          TAG='${{ steps.ver.outputs.tag }}'
          gh release view "$TAG" --json assets --jq '.assets[] | "\(.name)\t\(.size)"' > remote-assets.tsv
          echo "=== release 现有资产（共 $(wc -l < remote-assets.tsv) 个）==="
          cat remote-assets.tsv
          for f in release-assets/*; do
            name=$(basename "$f")
            size=$(stat -c%s "$f")
            remote=$(awk -F'\t' -v n="$name" '$1==n {print $2}' remote-assets.tsv)
            if [ -z "$remote" ]; then
              echo "::error::资产未出现在 release 上: $name"
              exit 1
            fi
            if [ "$remote" != "$size" ]; then
              echo "::error::资产大小不一致: $name local=$size remote=$remote"
              exit 1
            fi
            echo "OK: $name ($size bytes)"
          done
          # 边车收口：远端边车数量必须与本地一致（缺失=上传被吞或重名被删）
          remote_side=$(awk -F'\t' '$1 ~ /\.sha256$/ {n++} END {print n+0}' remote-assets.tsv)
          local_side=$(find release-assets -maxdepth 1 -type f -name '*.sha256' | wc -l)
          if [ "$remote_side" -ne "$local_side" ]; then
            echo "::error::远端边车数量 ${remote_side} != 本地边车数量 ${local_side}——自动更新校验链不完整"
            exit 1
          fi
          echo "OK: ${remote_side} 个边车已随主资产上传并核对"
          echo "=== 全部资产上传并核对通过 ==="

  # ── Gitee 镜像（自动更新第二源）─────────────────────────────────────────
  # 客户端壳侧更新器轮询 GitHub + Gitee 双源 releases/latest，国内用户主要打
  # Gitee。镜像范围=GitHub release 全部资产（含 .sha256 边车），约束与策略：
  #   · Gitee 单附件上限 100MB（104857600 字节）——超限主资产（v0.6.x 起
  #     win-x64 安装包 ~110MB 即超）按 `<原名>.part1/.part2/…` 分片镜像
  #     （每片 ≤80MiB；壳侧更新器 v0.6.3 起支持分片识别、逐片拼接后按
  #     `<原名>.sha256` 边车整文件校验）——此前「跳过+回落 GitHub」对 GitHub
  #     不可达的用户等于永久无法自动更新（v0.6.2 实爆）；边车均为小文件
  #     不受限，必须全部镜像。
  #   · Gitee v5 API 无「删除单个附件」端点（重复上传会产生重复条目），幂等
  #     策略：同名同大小→跳过；同名不同大小→删除整个 Gitee release 重建
  #     （对齐 GitHub 侧 delete-then-upload 纪律；dispatch 重建资产后重跑
  #     本 job 即完成镜像同步；重建同时清除陈旧分片，防资产缩小后残片污染
  #     客户端拼接——v5 无删单附件端点，残片只能靠重建清理）。
  #   · Gitee 侧自动附带 tag 源码包（v*.zip / v*.tar.gz），属平台行为不算缺失。
  #   · 依赖仓库 secret：GITEE_TOKEN（gitee.com 私人令牌，需 projects 权限）。
  mirror-gitee:
    name: Mirror Release to Gitee
    needs: [publish]
    if: false  # dual-track: 本机只需 Windows x64，短路省 CI 分钟
    runs-on: ubuntu-latest
    timeout-minutes: 75
    env:
      GH_REPO: ${{ github.repository }}
      GITEE_API: https://gitee.com/api/v5
      GITEE_REPO: my-yang-yunfan/dsh_desktop
      GITEE_FILE_LIMIT: '83886080'
    steps:
      - name: Resolve release tag/version
        id: ver
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            tag='${{ github.event.inputs.version }}'
            case "$tag" in v*) ;; *) tag="v$tag" ;; esac
          else
            tag='${{ github.ref_name }}'
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=${tag#v}" >> "$GITHUB_OUTPUT"

      - name: Guard GITEE_TOKEN
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          if [ -z "$GITEE_TOKEN" ]; then
            echo "::error::缺少 secret GITEE_TOKEN——请在 仓库 Settings → Secrets and variables → Actions 配置 gitee.com 私人令牌（需 projects 权限），然后重跑本 job"
            exit 1
          fi
          echo "GITEE_TOKEN 已配置"

      - name: Checkout (for tag push fallback)
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.ver.outputs.tag }}
          fetch-depth: 1

      - name: Ensure tag exists on Gitee
        # Gitee 建_release 依赖 tag 已存在。常见情形是 Gitee 仓库已通过镜像同步
        # 拿到 tag（直接跳过 push）；否则用 GITEE_TOKEN 作 HTTPS 密码浅取 tag
        # 推过去；再不行给出人工路径（runbook §自动更新配套）。
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          set -euo pipefail
          TAG='${{ steps.ver.outputs.tag }}'
          # 本地建立 tag ref（checkout 检出的是 commit，未必带 tag 引用）
          git fetch --no-tags --depth=1 origin "refs/tags/${TAG}:refs/tags/${TAG}" || {
            echo "::error::无法从 GitHub 取 tag ${TAG}（tag 不存在？）"; exit 1; }
          if curl -sf "${GITEE_API}/repos/${GITEE_REPO}/tags?access_token=${GITEE_TOKEN}&per_page=100" \
               | jq -e --arg t "$TAG" '[.[].name] | index($t)' >/dev/null; then
            echo "Gitee 已有 tag ${TAG}（镜像同步或此前推送）——跳过 push"
            exit 0
          fi
          echo "Gitee 缺 tag ${TAG}——通过 HTTPS + token 推送"
          LOGIN=$(curl -sf "${GITEE_API}/user?access_token=${GITEE_TOKEN}" | jq -r '.login')
          [ -n "$LOGIN" ] && [ "$LOGIN" != "null" ] || { echo "::error::GITEE_TOKEN 无法换取用户名（/user 失败）"; exit 1; }
          git remote add gitee "https://${LOGIN}:${GITEE_TOKEN}@gitee.com/${GITEE_REPO}.git"
          # remote URL 含 token：push --quiet 且失败时先脱敏再回显
          if ! git push --quiet gitee "refs/tags/${TAG}" 2>/tmp/push.err; then
            echo "::error::推送 tag 到 Gitee 失败（Gitee 仓库可能落后于 GitHub，浅推 tag 缺上游对象）。"
            echo "人工处理：Gitee 仓库管理→强制同步，或本地 git push gitee ${TAG} 后重跑本 job"
            cat /tmp/push.err | sed "s/${GITEE_TOKEN}/***/g" || true
            exit 1
          fi
          echo "OK: tag ${TAG} 已推送到 Gitee"

      - name: Download GitHub release assets
        # 从 GitHub release 下载全部资产作为镜像源（含 .sha256 边车）——
        # 以上传核对过的远端为准，而非本地构建产物。
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG='${{ steps.ver.outputs.tag }}'
          mkdir -p gh-assets
          gh release download "$TAG" --dir gh-assets --clobber
          echo "=== 待镜像资产 ==="
          ls -la gh-assets

      - name: Get-or-rebuild Gitee release
        # 幂等核心：同名同大小→复用现有 release；任何同名不同大小（dispatch
        # 重建过资产）→ 删整个 Gitee release 重建（v5 无删单附件 API）；
        # 不存在→按 GitHub 元数据创建。
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          set -euo pipefail
          TAG='${{ steps.ver.outputs.tag }}'
          # GitHub 侧元数据（镜像以 GitHub 为准源）
          gh release view "$TAG" --json name,body,isPrerelease > gh-meta.json
          NAME=$(jq -r '.name' gh-meta.json)
          BODY=$(jq -r '.body' gh-meta.json)
          PRE=$(jq -r '.isPrerelease' gh-meta.json)
          http=$(curl -s -o gitee-rel.json -w '%{http_code}' "${GITEE_API}/repos/${GITEE_REPO}/releases/tags/${TAG}?access_token=${GITEE_TOKEN}")
          create_release() {
            curl -sf -X POST "${GITEE_API}/repos/${GITEE_REPO}/releases" \
              --data-urlencode "access_token=${GITEE_TOKEN}" \
              --data-urlencode "tag_name=${TAG}" \
              --data-urlencode "name=${NAME}" \
              --data-urlencode "body=${BODY}" \
              --data-urlencode "prerelease=${PRE}" \
              --data-urlencode "target_commitish=${TAG}" > gitee-rel.json
          }
          # Gitee 对不存在的 release 返回 200 + 空(null) 而非 404（实测），
          # 以响应体能否取到 .id 判定存在性，勿只依赖 HTTP 状态码。
          EXISTS_ID=$(jq -r 'if type == "object" then (.id // empty) else empty end' gitee-rel.json 2>/dev/null || true)
          if [ "$http" = "200" ] && [ -n "$EXISTS_ID" ]; then
            REL_ID="$EXISTS_ID"
            echo "Gitee release ${TAG} 已存在（id=${REL_ID}）——核对资产漂移"
            drift=0
            while IFS=$'\t' read -r name size; do
              remote=$(jq -r --arg n "$name" '.assets[]? | select(.name==$n) | .size' gitee-rel.json | head -1)
              if [ -n "$remote" ] && [ "$remote" != "null" ] && [ "$remote" != "$size" ]; then
                echo "漂移: $name 本地=${size} Gitee=${remote}（同名不同大小）"
                drift=1
              fi
            done < <(cd gh-assets && for f in *; do printf '%s\t%s\n' "$f" "$(stat -c%s "$f")"; done)
            if [ "$drift" -eq 1 ]; then
              echo "同名资产内容不一致——删除整个 Gitee release 重建（v5 无删单附件端点）"
              curl -sf -X DELETE "${GITEE_API}/repos/${GITEE_REPO}/releases/${REL_ID}?access_token=${GITEE_TOKEN}"
              create_release
            fi
          elif [ "$http" = "200" ] || [ "$http" = "404" ]; then
            echo "Gitee release ${TAG} 不存在（http=${http}）——按 GitHub 元数据创建"
            create_release
          else
            echo "::error::查询 Gitee release 失败 http=${http}"; cat gitee-rel.json; exit 1
          fi
          REL_ID=$(jq -r '.id' gitee-rel.json)
          [ -n "$REL_ID" ] && [ "$REL_ID" != "null" ] || { echo "::error::未取得 Gitee release id"; cat gitee-rel.json; exit 1; }
          echo "REL_ID=$REL_ID" >> "$GITHUB_ENV"
          echo "OK: Gitee release id=${REL_ID}"

      - name: Upload assets to Gitee
        # 上传缺失资产；超限（>80MiB）资产按 `<原名>.partN` 分片镜像；
        # 已存在同名同大小跳过（幂等）。边车是小文件，永远在镜像范围内。
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          set -euo pipefail
          REL_ID='${{ env.REL_ID }}'
          LIMIT=${GITEE_FILE_LIMIT}
          # 刷新远端资产清单（rebuild 后为空，复用时为现状）
          curl -sf "${GITEE_API}/repos/${GITEE_REPO}/releases/tags/${{ steps.ver.outputs.tag }}?access_token=${GITEE_TOKEN}" > gitee-now.json
          # 上传顺序：win-x64 安装包优先（CN 用户主下载资产），其后其余资产。
          # 单资产最终失败不再中断整个镜像——上一版 arm64 exe 连败两次即 exit，
          # win-x64 安装包永远轮不到上传（Gitee 源全用户更新失效的实爆）。
          # 收口由下方 Verify mirrored assets 兜底：缺失/大小不符仍会拒发。
          declare -A SEEN
          FAIL=0
          # 带重试的原生上传（路径级原语：不做存在性/大小判定）。
          # Gitee 上传 API 大文件慢：单文件 10 分钟上限 + 4 次退避重试
          #（20/40/60s——v0.5.7 首发实测单文件偶发连败两次即退）。
          upload_raw() {
            local f="$1" name attempt
            name=$(basename "$f")
            echo "上传到 Gitee: $name（$(stat -c%s "$f") 字节）"
            for attempt in 1 2 3 4; do
              if curl -sf --max-time 600 -X POST "${GITEE_API}/repos/${GITEE_REPO}/releases/${REL_ID}/attach_files?access_token=${GITEE_TOKEN}" \
                -F "file=@${f}" > /dev/null; then
                echo "  上传成功: $name（第 ${attempt} 次）"
                return 0
              fi
              echo "::warning::上传失败（第 ${attempt} 次）: $name"
              if [ "$attempt" -eq 4 ]; then return 1; fi
              sleep $((attempt * 20))
            done
            return 1
          }
          upload_one() {
            local f="$1" name size remote
            name=$(basename "$f")
            if [ -n "${SEEN[$name]:-}" ]; then return 0; fi
            SEEN[$name]=1
            size=$(stat -c%s "$f")
            # Gitee v5 资产对象无 size 字段（只有 name + browser_download_url），
            # 存在性按 name 判定；大小由 sha256 边车兜底。
            remote=$(jq -r --arg n "$name" '.assets[]? | select(.name==$n) | .name' gitee-now.json | head -1)
            if [ -n "$remote" ] && [ "$remote" != "null" ]; then
              echo "跳过（已镜像）: $name"
              return 0
            fi
            if [ "$size" -gt "$LIMIT" ]; then
              # 超 Gitee 单附件限：按 `<原名>.partN` 分片镜像（每片 ≤LIMIT、
              # 片号从 1 起无补零、分片数 ≤9）；壳侧 v0.6.3+ 逐片拼接后按
              # `<原名>.sha256` 边车整文件校验。任一片失败 → 整体失败
              # （半套分片 = 客户端拼接必坏，宁可拒发）。幂等：逐片同名跳过。
              local parts_dir="parts-$$" pc p
              rm -rf "$parts_dir"; mkdir -p "$parts_dir"
              split -b "$LIMIT" -a 1 --numeric-suffixes=1 "$f" "$parts_dir/$name.part"
              pc=$(ls "$parts_dir" | wc -l)
              if [ "$pc" -gt 9 ]; then
                echo "::error::分片数 $pc > 9 超出命名宽度（-a 1）：$name——需调大 -a 或 LIMIT"
                rm -rf "$parts_dir"; return 1
              fi
              for p in "$parts_dir"/*; do
                upload_raw "$p" || { rm -rf "$parts_dir"; return 1; }
              done
              # 手动救援脚本：与分片同目录摆放后双击合并（GitHub/Gitee 双源
              # 都不通的老壳用户——0.4.x 实爆形态——只能手动下载；merge.bat
              # + certutil 哈希核对让纯手动路径也可自证完整性）。CRLF + ASCII
              # （cmd 对 LF 容忍但别赌；中文在老代码页会乱码）。
              local plist=""
              for p in "$parts_dir"/*.part[0-9]; do
                plist="${plist}$(basename "$p")+"
              done
              plist="${plist%+}"
              printf '@echo off\r\n' > "$parts_dir/$name.merge.bat"
              printf 'rem DSH Desktop split-asset merger (auto-generated by release pipeline)\r\n' >> "$parts_dir/$name.merge.bat"
              printf 'rem Keep this .bat together with the .partN files, then double-click to merge.\r\n' >> "$parts_dir/$name.merge.bat"
              printf 'setlocal\r\n' >> "$parts_dir/$name.merge.bat"
              printf 'cd /d "%%~dp0"\r\n' >> "$parts_dir/$name.merge.bat"
              printf 'copy /b %s "%s" >nul\r\n' "$plist" "$name" >> "$parts_dir/$name.merge.bat"
              printf 'if errorlevel 1 (\r\n  echo Merge failed: check all %d parts are in this folder.\r\n  pause\r\n  exit /b 1\r\n)\r\n' "$pc" >> "$parts_dir/$name.merge.bat"
              printf 'echo Merged OK: %s\r\n' "$name" >> "$parts_dir/$name.merge.bat"
              printf 'echo Compare SHA256 below with %s.sha256:\r\n' "$name" >> "$parts_dir/$name.merge.bat"
              printf 'certutil -hashfile "%s" SHA256\r\n' "$name" >> "$parts_dir/$name.merge.bat"
              printf 'pause\r\n' >> "$parts_dir/$name.merge.bat"
              upload_raw "$parts_dir/$name.merge.bat" || { rm -rf "$parts_dir"; return 1; }
              rm -rf "$parts_dir"
              echo "  分片镜像完成: $name（${size} 字节 → ${pc} 片 + merge.bat）"
              return 0
            fi
            upload_raw "$f"
          }
          for f in gh-assets/*-win-x64.exe gh-assets/*; do
            if [ -f "$f" ]; then upload_one "$f" || FAIL=1; fi
          done
          if [ "$FAIL" -ne 0 ]; then
            echo "::error::一个或多个资产最终上传失败（详见上方告警）——Verify 步骤将复核并拒绝发布"
            exit 1
          fi

      - name: Verify mirrored assets
        # 镜像收口：重查 Gitee release，每个 ≤limit 的源资产（含全部边车）
        # 必须存在且大小与 GitHub 侧逐字节一致；超限资产必须以完整
        # `<原名>.part1..N` 分片套在场（缺片/残片均拒发）。
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          set -euo pipefail
          TAG='${{ steps.ver.outputs.tag }}'
          LIMIT=${GITEE_FILE_LIMIT}
          curl -sf "${GITEE_API}/repos/${GITEE_REPO}/releases/tags/${TAG}?access_token=${GITEE_TOKEN}" > gitee-final.json
          echo "=== Gitee 镜像核对（tag ${TAG}）==="
          fail=0
          mirrored=0
          for f in gh-assets/*; do
            name=$(basename "$f")
            size=$(stat -c%s "$f")
            if [ "$size" -gt "$LIMIT" ]; then
              # 超限资产：分片套完整性校验。片号 1..N 全在场 + 无多余残片
              # （残片会把客户端拼接成错误字节流——哈希必炸，宁可此处拒发；
              # v5 无删单附件端点，清理路径 = 删整个 release 重跑本 job）。
              n=$(( (size + LIMIT - 1) / LIMIT ))
              part_missing=0
              for i in $(seq 1 "$n"); do
                remote=$(jq -r --arg pn "$name.part$i" '.assets[]? | select(.name==$pn) | .name' gitee-final.json | head -1)
                if [ -z "$remote" ] || [ "$remote" = "null" ]; then
                  echo "::error::Gitee 缺失分片: $name.part$i（${i}/${n}）"
                  fail=1
                  part_missing=1
                fi
              done
              remote_parts=$(jq -r --arg base "$name.part" '[.assets[]?.name | select(startswith($base))] | length' gitee-final.json)
              if [ "$remote_parts" -gt "$n" ]; then
                echo "::error::Gitee 分片数 ${remote_parts} > 期望 ${n}（$name——陈旧残片，重建 Gitee release 后重跑本 job）"
                fail=1
              elif [ "$part_missing" -eq 0 ]; then
                printf '%-52s %12d bytes  [分片镜像 x%d OK]\n' "$name" "$size" "$n"
              fi
              continue
            fi
            remote=$(jq -r --arg n "$name" '.assets[]? | select(.name==$n) | .name' gitee-final.json | head -1)
            if [ -z "$remote" ] || [ "$remote" = "null" ]; then
              echo "::error::Gitee 缺失资产: $name（${size} 字节，未超限不应缺失）"
              fail=1
              continue
            fi
            printf '%-52s %12d bytes  OK\n' "$name" "$size"
            mirrored=$((mirrored + 1))
          done
          # 边车必须全部在镜像内（小文件永不受限）
          side_total=$(find gh-assets -maxdepth 1 -name '*.sha256' | wc -l)
          side_mirrored=$(jq -r '[.assets[]?.name | select(endswith(".sha256"))] | length' gitee-final.json)
          if [ "$side_mirrored" -lt "$side_total" ]; then
            echo "::error::Gitee 边车不全：${side_mirrored}/${side_total}（边车是小文件，缺=镜像链路故障）"
            fail=1
          fi
          [ "$fail" -eq 0 ] || exit 1
          echo "OK: ${mirrored} 个资产镜像一致（含 ${side_mirrored} 个边车；超限资产以 .partN 分片镜像）"
